<h1>Listado de categor&iacute;as de soporte</h1> 
  <table> 
    <tr> 
     <th>Categor&iacute;a</th> 
     <th></th> 
     <th></th> 
    </tr> 
   <% @categories_assignations.each do |category| %> 
    <tr>  
     <td><%= category.name %></td> 
     <td>
       <% if can? :update, category %>
         <%= link_to 'Edit', edit_category_path(category) %>
      <% end %>
     </td> 
     <td>
      <% if can? :delete, category %> 
         <%= link_to 'Destroy', category, confirm: 'Are you sure?', method: :delete %>
      <% end %> 
     </td> 
    </tr> 
   <% end %> 
</table> 
<br />
<%= link_to 'Agregar nueva categoría', new_category_path %>
